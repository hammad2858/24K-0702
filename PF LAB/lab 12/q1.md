What if I try to allocate the memory like this
int n;
scanf(“%d”,&n);
int arr[n]
**Q)Is this a compile time memory allocation or runtime memory allocation?**

**Answer: the compiler cannot determine the size of the array arr[n] at compile time because the value of n is only known at runtime**
